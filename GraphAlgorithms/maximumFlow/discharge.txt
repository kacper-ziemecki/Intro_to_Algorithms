discharge(u)
while u.e > 0
  v = u.current 
  if v == NIL  
    RELABEL(u)
    u.current = u.N.head 
  else if c f (u, v) > 0 and u.h == v.h + 1 
    PUSH(u, v)
  else 
    u.current = v.next-neighbor